{
  "name": "zapclick",
  "version": "1.0.0",
  "description": "Aplicativo de automação do WhatsApp",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "npx electron-packager . ZapClick --platform=win32 --arch=x64 --out=dist --overwrite",
    "vercel-build": "echo 'Building for Vercel...'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "whatsapp",
    "automation",
    "electron"
  ],
  "author": "Sthevan",
  "license": "ISC",
  "dependencies": {
    "electron": "^28.1.0",
    "puppeteer": "^21.5.0",
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "electron-packager": "^17.1.2"
  }
}
